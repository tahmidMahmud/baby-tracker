{
  "name": "baby-tracker",
  "private": true,
  "description": "Free PWA baby tracker — static app, no build step. Tests run on Node's built-in runner.",
  "scripts": {
    "test": "TZ=America/New_York node --test tests/*.test.mjs"
  }
}
